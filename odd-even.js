const checkEvenOdd = (number) => {
    if (number % 2 === 0) {
        console.log(`${number} is an even number;`);
    }else {
        console.log(`${number} is an odd number;`);
    }
}

checkEvenOdd(5);
checkEvenOdd(10);